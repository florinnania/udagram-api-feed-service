apiVersion: v1
kind: Service
metadata:
  name: udagram-api-feed-service
  labels:
    run: udagram-api-feed-service
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    run: udagram-api-feed-service